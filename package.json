{
  "name": "yarden-webhook-server",
  "version": "1.0.0",
  "description": "Instagram webhook server",
  "main": "src/webhook-server.js",
  "scripts": {
    "start": "node src/webhook-server.js",
    "serve": "http-server . -p 4173 -c-1 --silent",
    "test:e2e": "playwright test",
    "test:report": "playwright show-report"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "http-server": "^14.1.1"
  },
  "engines": {
    "node": ">=18"
  }
}