# Site Architecture — yardendamri.co.il
*Last updated: 2026-06-21*

---

## Overview

Static website for Yarden Damri, Israeli bridal & evening makeup artist. Hosted on GitHub Pages. Media (images + videos) stored on Cloudflare R2 and served via custom domains. Admin settings synced via a Cloudflare Worker. Instagram feed auto-synced every 6 hours via GitHub Actions.

---

## Services Used & Why

| Service | Role | Why |
|---------|------|-----|
| **GitHub Pages** | Hosts the static site | Free, reliable, Git-based deployment |
| **Cloudflare R2** | Stores all images and videos | Free storage + free egress, no bandwidth limits |
| **Cloudflare Worker** | Admin API (`api.yardendamri.co.il`) | Serverless, reads/writes `gallery-settings.json` in GitHub with auth |
| **Cloudflare DNS** | Domain routing for all subdomains | Site already on Cloudflare, unified control |
| **GitHub Actions** | Instagram sync every 6 hours | Free CI/CD, runs `fix.js` to fetch + compress + upload new posts |
| ~~Cloudinary~~ | ~~Old media host~~ | **CLOSED** — exceeded free plan limit June 2026 |
| ~~ImageKit~~ | ~~Old image optimizer~~ | **SUSPENDED** — all ImageKit URLs (images + video) now return 403; fully retired, do not reuse |
| ~~Render.com~~ | ~~Old Instagram proxy~~ | **REMOVED** — dead code, replaced by GitHub Actions |

---

## Why Two R2 Buckets?

| Bucket | Domain | Contains | Why Separate |
|--------|--------|----------|--------------|
| `yarden-images` | `images.yardendamri.co.il` | All photos (.webp) + video thumbnails (`_thumb.jpg`) | Images need different cache/access rules than videos |
| `yarden-videos-new` | `videos-new.yardendamri.co.il` | All videos (.mp4) | Videos are large files, separated to allow future CDN/streaming rules |

> **Note:** There is also an old `yarden-videos` bucket (`videos.yardendamri.co.il`) with the original pre-compression videos. It is kept as backup but no longer actively used. The hero video currently points to it — migrate when thumbnails are generated.

---

## Repository Structure

**Repo:** `github.com/ofirdamr/yarden-damri` (username: `ofirdamr`, branch: `main`)

```
/ (root)                          ← LIVE SITE (yardendamri.co.il)
  index.html                      ← Homepage with hero video
  gallery.html                    ← Gallery page
  gallery-data.js                 ← All media items (auto-generated by sync, copy of preview/)
  gallery-settings.json           ← Admin settings: categories, hidden, pinned, hero, likes, comments
  instagram-stats.json            ← Instagram likes/comments per post (auto-generated by sync)
  admin.html                      ← Admin panel (password protected via Worker)
  about.html, services.html, pricing.html, reviews.html,
  bride.html, bridal-guide.html, contact.html
  styles.css                      ← Global styles
  ARCHITECTURE.md                 ← This file
  CNAME                           ← yardendamri.co.il
  robots.txt, sitemap.xml

/preview/                         ← STAGING SITE (yardendamri.co.il/preview/)
  (mirrors root — all active development happens here)
  gallery-data.js                 ← Source of truth (sync writes here first)
  instagram-stats.json            ← Source of truth (sync writes here first)
  cloud-storage.js                ← RemoteState API (reads Worker, caches in memory)
  worker.js                       ← Cloudflare Worker source code
  admin.html                      ← Admin panel
  SUMMARY.md                      ← Current project status
  PROGRESS.md                     ← Work log
  MISTAKES.md                     ← Error log

/.github/workflows/
  sync-auto.yml                   ← Instagram sync (every 6h + manual trigger)
  cleanup.yml                     ← Cleanup job

/fix.js                           ← Sync script (Instagram → compress → R2 → gallery-data.js)
```

---

## Data Flow

### Instagram Sync (every 6 hours)
```
Instagram API
    ↓ fetch all media (photos + videos + carousel children)
fix.js (GitHub Actions runner)
    ↓ skip items already in gallery-data.js (fast path)
    ↓ NEW images: download → compress WebP 800px → upload to yarden-images R2
    ↓ NEW videos: download → compress H.264 720p → upload to yarden-videos-new R2
    ↓             also extract first frame → upload as _thumb.jpg to yarden-images R2
    ↓ fetch Instagram stats (likes/comments) for all items
    ↓ write preview/gallery-data.js + preview/instagram-stats.json
    ↓ copy both files to root (gallery-data.js, instagram-stats.json)
    → git commit + push → GitHub Pages picks up in ~2 min
```

### Admin Settings Flow
```
Admin opens /preview/admin.html
    ↓ loads cloud-storage.js (RemoteState)
    ↓ Admin enters password → POST api.yardendamri.co.il/login
    ↓ Worker validates password, returns 64-char session token (stored in sessionStorage)
    ↓ initAdmin() → fetchPublic() → fetches gallery-settings.json from GitHub Pages URL
Admin makes change (hide photo, set category, change hero video)
    ↓ RemoteState.update({ admin: {...} })
    ↓ POST api.yardendamri.co.il/settings with Authorization: Bearer <token>
    ↓ Worker validates token against KV, reads current gallery-settings.json from GitHub API
    ↓ merges changes, writes back to GitHub
    ↓ (if hero video changed) Worker also patches preview/index.html src + poster directly
All visitors see change on next page load (gallery-settings.json re-fetched)
```

### Gallery Page Load (visitor)
```
Browser loads gallery.html
    ↓ immediately renders with whatever is cached (fast first paint)
    ↓ loadIgFeed() → fetch instagram-stats.json (cache: no-store, always fresh)
    ↓ RemoteState.fetchPublic() → fetch gallery-settings.json
    ↓ re-renders gallery with: hidden items removed, categories applied, likes/comments shown
    ↓ IntersectionObserver: videos autoplay only when in viewport
```

---

## Media URL Patterns

```
Photos:           https://images.yardendamri.co.il/yarden_{itemId}.webp
Video thumbnails: https://images.yardendamri.co.il/yarden_{itemId}_thumb.jpg
Videos:           https://videos-new.yardendamri.co.il/yarden_{itemId}.mp4
```

---

## Admin API (Cloudflare Worker)

- **URL:** `https://api.yardendamri.co.il`
- **Source:** `preview/worker.js` — deployed via `.github/workflows/deploy-worker.yml` (CF REST API)
- **Script name:** `yarden-admin`
- **KV binding:** `SESSIONS` → namespace `yarden-admin-sessions` (ID: `7fc38ac017a145fea0a486419a3bff07`)
- **Secrets:** `ADMIN_PASSWORD`, `GH_TOKEN` (set in Cloudflare dashboard)

| Endpoint | Auth | Purpose |
|----------|------|---------|
| `GET /settings` | None (public) | Returns `gallery-settings.json` |
| `POST /login` | Password in body | Validates password, returns 8h session token |
| `POST /logout` | `Authorization: Bearer <token>` | Invalidates session token |
| `POST /settings` | `Authorization: Bearer <token>` | Merges partial update into `gallery-settings.json` |
| `GET /social` | None (public) | Returns likes + comments |
| `POST /social` | None (public) | Updates likes + comments |
| `GET /s/<v\|p>/<id>` | None (public) | Share page: OG card (thumbnail + "לחצי כאן לצפייה" title) → redirects to `gallery.html?m=<id>` (`?g=t` → gallery-temp). `v`=video (thumb jpg), `p`=photo (webp) |

**Rate limiting:** 5 failed `/login` attempts → 15-min IP lockout (KV key `rl:{ip}`)

**Deploy safety:** `deploy-worker.yml` metadata includes `{"type":"inherit","name":"ADMIN_PASSWORD"}` and `GH_TOKEN` so re-deploying the script does NOT wipe the existing secret bindings.

### gallery-settings.json structure
```json
{
  "admin": {
    "cats": { "https://images.../yarden_X.webp": "כלות/חתונות" },
    "hidden": ["https://images.../yarden_X.webp"],
    "pinned": [],
    "order": [],
    "catList": ["כלות/חתונות", "איפור ערב", "הפקות", "עור כהה", "ציורי גוף ופנים"],
    "privateCats": ["מבוגרות", "מוסתרות", "תמונות שלי", "איפור פורים", "נשף"],
    "rotations": {},
    "heroZoom": 110
  },
  "heroVideo": "https://videos.yardendamri.co.il/yarden_makeup_18352618189204100.mp4",
  "heroImage": null,
  "heroPosition": "56% 58%",
  "likes": {},
  "comments": {},
  "reviews": [],
  "pricing": null
}
```

---

## Gallery Data Structure (gallery-data.js)

```js
const GALLERY_IMAGES = [
  {
    "u": "https://images.yardendamri.co.il/yarden_18095410015962853.webp",  // media URL
    "a": "caption text...",                                                   // Instagram caption
    "item_id": "18095410015962853",                                           // Instagram child media ID
    "post_id": "18399908116155226"                                            // Instagram parent post ID (for stats lookup)
  },
  {
    "u": "https://videos-new.yardendamri.co.il/yarden_18124163572566762.mp4",
    "a": "caption...",
    "item_id": "18124163572566762",
    "post_id": "18124163572566762",
    "video": true,
    "thumb": "https://images.yardendamri.co.il/yarden_18124163572566762_thumb.jpg"
  }
]
```

**Optional flags added by `fix.js`:**
- `hidden: true` — item kept in data (stays on R2 / visible to admin) but filtered out of public pages.
- `carousel: true`, `cidx: <n>`, `ccount: <n>` — child of a multi-image post. The gallery shows ONE cover tile per `post_id` (with a ⧉ badge) and the lightbox swipes the children.

**Gallery rendering (gallery.html / index.html):**
- `lbImages` = full expanded list (carousel children individual) → drives the lightbox.
- `allFilteredImages` / `filteredImages` = collapsed (one cover per carousel) → drives the grid.
- Deep link `?m=<item_id|post_id>` opens that exact item in the lightbox.
- Video grid tiles: `<img data-video>` thumbnail → swapped to autoplay `<video>` (poster=thumb) on scroll via IntersectionObserver.

---

## GitHub Actions Secrets Required

| Secret | Used For |
|--------|----------|
| `INSTAGRAM_TOKEN` | Instagram Graph API access |
| `R2_ACCESS_KEY_ID` | Cloudflare R2 authentication |
| `R2_SECRET_ACCESS_KEY` | Cloudflare R2 authentication |
| `R2_ENDPOINT` | R2 endpoint URL |
| `CF_WORKERS_API_TOKEN` | Deploy Worker via Cloudflare REST API (`deploy-worker.yml`) |

---

## Known Issues / To Do

- **Lightbox button layout (OPEN — next session)**: see `preview/NEXT-LIGHTBOX.md`. Lightbox media is fullscreen and the nav is hidden while open, but our custom buttons (close X, prev/next, like/comment/share action bar) overlap the browser's NATIVE `<video controls>` (mute top-left, PiP/fullscreen top-right, scrubber bottom). Needs a non-overlapping layout (likely drop native controls and use only our own).
- **About image**: served from R2 `https://images.yardendamri.co.il/about-yarden.png` (homepage + about page). The old ImageKit URL was dead (403) and has been replaced.
- **Hero video thumbnails**: `_thumb.jpg` not yet generated for existing 161 videos — admin picker shows black tiles, hero has brief dark flash before video plays.
- **Old `yarden-videos` bucket**: Still has original uncompressed videos. Hero video still points there. Should migrate to `yarden-videos-new` after thumbnails are ready.
- **Go-live**: Promote `/preview` → root following `GO-LIVE.md` (9-step checklist).
- **SEO**: Meta tags, sitemap, structured data — pending before go-live.
- **Online reservations**: Post-launch feature.
- **Google Places API**: Live reviews — post-launch.

## Stats Lookup — Important Note

`instagram-stats.json` is keyed by **post_id** (parent post). `gallery-data.js` items have both `item_id` (child media) and `post_id`. All `getIgStats()` functions must check `post_id` first, then `item_id`, then `id`. This was the root cause of likes/comments not showing on the homepage (fixed June 2026).

---

## Development Rules

1. **All changes go to `/preview` first** — never touch root files until go-live
2. **Draft Protection**: create `-temp` files, get approval, then overwrite
3. **Never use localStorage/sessionStorage** — Ofir uses iPhone incognito, both unavailable
4. **Never use Cloudinary** — account closed
5. **Never use ImageKit** — limits exceeded
6. **R2 is storage + delivery, no optimization** — compression done at upload time in `fix.js`
7. **Worker password never in repo** — stored as Cloudflare Worker environment variable
8. **GitHub token never in repo** — stored as Worker env var + project system prompt only
9. **Always sync root = preview** for `gallery-data.js` and `instagram-stats.json`
